use std::env;
use std::fs;
use std::path::Path;

// Now you can import and use types from your rust_lib!
use rust_lib::FFITy;

fn main() {
    println!("Build tools starting...");

    println!("Build tools completed successfully!");
}
